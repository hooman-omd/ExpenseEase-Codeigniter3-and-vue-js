<!DOCTYPE html>
<html lang="fa" dir="rtl">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
		<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
		<title>مدیریت هزینه و درآمد</title>
		<script src="https://cdn.tailwindcss.com"></script>
		<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.9.4/Chart.js"></script>
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
	</head>
	<body
		class="bg-gray-50 font-sans">
		<!-- نوار کناری -->
		<div class="flex h-screen">
			<aside class="w-64 bg-white shadow-md">
				<div class="p-4 border-b border-gray-200">
					<h1 class="text-xl font-bold text-indigo-600">مدیریت مالی</h1>
				</div>
				<nav class="p-4">
					<ul class="space-y-2">
						<li>
							<a href="{{base_url('Dashboard/index')}}" class="flex items-center p-2 text-gray-700 rounded-lg hover:bg-indigo-50">
								<i class="fas fa-home ml-2"></i>
								<span>داشبورد</span>
							</a>
						</li>
						<li>
							<a href="{{base_url('Transactions/index')}}" class="flex items-center p-2 text-gray-700 rounded-lg hover:bg-indigo-50">
								<i class="fas fa-exchange-alt ml-2"></i>
								<span>تراکنش‌ها</span>
							</a>
						</li>
						{# <li>
												                        <a href="#" class="flex items-center p-2 text-gray-700 rounded-lg hover:bg-indigo-50">
												                            <i class="fas fa-chart-line ml-2"></i>
												                            <span>گزارش‌ها</span>
												                        </a>
												                    </li> #}
						<li>
							<a href="{{base_url('Category/index')}}" class="flex items-center p-2 text-gray-700 rounded-lg hover:bg-indigo-50">
								<i class="fas fa-tags ml-2"></i>
								<span>دسته‌بندی‌ها</span>
							</a>
						</li>
						<li>
							<a href="{{base_url('User/index')}}" class="flex items-center p-2 text-gray-700 rounded-lg hover:bg-indigo-50">
								<i class="fas fa-user ml-2"></i>
								<span>پروفایل</span>
							</a>
						</li>
					</ul>
				</nav>
			</aside>

			<!-- محتوای اصلی -->
			<main
				class="flex-1 overflow-y-auto">
				<!-- هدر -->
				<header class="bg-white shadow-sm p-4 flex justify-between items-center">
					<h2 class="text-lg font-semibold text-gray-800"> {% block pagetitle %}{% endblock %}
						</h2>
						<div class="flex items-center space-x-4">
							<div class="relative">
								<a href="{{base_url('Auth/logout')}}" id="logout-btn" class="bg-red-400 text-white ml-9 py-2 px-4 rounded-lg hover:bg-red-500 focus:outline-none focus:ring-2 focus:ring-indigo-500">
									<i class="fas fa-user-lock"></i>
									خروج از سیستم
								</a>
							</div>
							<div class="flex items-center">
								<img src="{% if session('url') is empty%}{{base_url()}}uploads/profile.avif{% else %}{{session('url')}}{% endif %}" alt="پروفایل" class="w-8 h-8 rounded-full">
								<span class="mr-2 text-sm">{{session('name')}}</span>
							</div>
						</div>
					</header>

					<!-- main content -->

					{% block content %}{% endblock %}

					<!-- end main content -->

				</main>
			</div>
		</body>
		<script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
	</html>
